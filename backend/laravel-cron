# 設定 PATH，讓 cron 找到指令
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# 每分鐘執行 Laravel schedule，指定 root 使用者
* * * * * root cd /var/www && /usr/local/bin/php artisan schedule:run >> /var/log/cron.log 2>&1
